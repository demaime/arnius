import type { FrontPageItem } from "@newsletter/core";

// Stub de la fase 0 — el scraper real llega en la fase 3.
const items: FrontPageItem[] = [];
console.log(`scraper stub ok — ${items.length} items`);
